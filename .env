# Point the diner app at the backend. Unset this and the app falls back to the
# in-browser stand-in in src/api/client.ts (which is what the smoke tests use).
VITE_API_URL=http://localhost:3100/api/v1
